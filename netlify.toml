[build]
  command = "npm run build"
  publish = "dist"                 # Vite output
  functions = "netlify/functions"  # where our functions live

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
